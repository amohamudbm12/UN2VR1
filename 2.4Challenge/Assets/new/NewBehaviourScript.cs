using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    int player = 4;
    Rigidbody2D man = null;
    void Start()
    {
        Debug.Log(player);
        Debug.Log(man);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
